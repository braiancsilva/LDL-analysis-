# ── Individual-level data (ADNI Data Use Agreement — never commit) ───────────
Data/
*.xlsx
*.xls
*.csv
*.rds
*.RData
*.rda

# ── Outputs (shareable only when deliberately promoted) ─────────────────────
Outputs/*
!Outputs/.gitkeep

# ── Credentials ─────────────────────────────────────────────────────────────
secrets/*
!secrets/*.example

# ── Local-only working documents (kept on disk, not committed) ──────────────
CLAUDE.md
STYLE.md
grilling.md

# ── R / RStudio ─────────────────────────────────────────────────────────────
.Rhistory
.RData
.Rproj.user/
*.Rproj
*.knit.md
*_files/

# ── System ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
